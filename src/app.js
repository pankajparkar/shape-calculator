function getShapes () {
  return [
    'Rectangle',
    'Circle',
    'Square',
    'Eclipse'
  ]
}

window.addEventListener('DOMContentLoaded', () => {
  console.log('Html Loaded...');

  // Options renderer
  const options = getShapes();
  
  // Ask for parameters

  // Show the result

});
